<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TOOL"
            android:label="@string/title_activity_tool"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LinkActivity"
            android:label="@string/title_activity_link" />
        <activity android:name=".Cake1" />
        <activity android:name=".Roll1" />
        <activity android:name=".Roll2" />
        <activity android:name=".Roll3" />
        <activity android:name=".Roll4" />
        <activity android:name=".Roll5" />
        <activity android:name=".Roll6"></activity>
    </application>

</manifest>